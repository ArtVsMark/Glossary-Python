гейт `scripts/check_defaults.py`: умолчание, взятое из окружения — кодировка у текстового файла и `text=True`, часовой пояс у `datetime.now()`, — отвергается разбором исходника, а не ловится прогоном (#25)
