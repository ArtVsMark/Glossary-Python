у находки в `objections.json` появилась область (`scope`) и `schema_of`, а вместо отметки времени — отпечаток снимка: находка уровня глоссария больше не теряется у потребителя, читающего только `cards`
